'use strict';
import React from 'react';
import data from '../movieData';

export default class App extends React.Component {
  render() {
    console.log(data);

    return (
      <div>app</div>
    );
  }
}
